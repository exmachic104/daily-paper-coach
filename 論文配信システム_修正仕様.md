# 論文配信システム 修正仕様

## 背景

3日サイクル・1日15分で論文1本を読み切る運用で、以下の実害が出た。

1. **指定範囲に答えが存在しない設問が出題された。** ある論文のQ1で、問いの片側（低速側の理由）は指定範囲内にあったが、対になるもう片側（高速側の理由）は指定範囲外（Conclusion）にしか記述がなかった。にもかかわらず全文ベースの模範解答で採点され、範囲を読んでも到達できない内容を要求された。
2. **1問に1時間かけても解けず、期限内に提出できなかった。** 制御理論寄りの論文（二次平均化・エネルギー関数ベースの飽和モデル）で、前提概念（微分逆インダクタンス行列、非線形可観測性、座標変換のサンドイッチ構造）が未習だったため停滞した。時間を延ばしても解決しない性質の停滞だった。

原因は、生成・出題・採点・日数割り当てがそれぞれ独立しており、整合を検証する仕組みがない点にある。

## 現状の実装（確認済み）

- `generate_delivery_and_quiz`: PDF全体をdocumentブロックで渡し、**全文から**読書プラン・Q1〜Q3・模範解答・`key_points`を配信日に一括生成。「その日までの範囲で答えられること」という制約はプロンプトで課しているが、検証はしていない。
- `grade_single`: `summary` / `key_points` / `question` / `model_answer` / `assigned_sections` のみを渡す。**PDFは渡していない**（コスト・レイテンシ削減のため）。
- `morning.py`: `cycle_day` 1→2→3 がハードコード。「15分」「10〜17分」「30〜50分」は生成プロンプト内の固定文言。論文の長さ・難易度から算出していない。

## 設計上の制約（変更しないこと）

- **1日15分はハード制約。** ユーザーが確保できる時間の上限であり、延ばせない。時間を可変にする方向の対策は採らない。
- **ユーザーによるトリアージ（論文の取捨選択）は実装しない。** どの論文を読むべきかの選択自体をAIに委託するのがこのシステムの思想であり、ユーザー側に判断を持たせる設計は思想・負荷の両面から不可。
- **キューから論文を捨てない。** 目的は関連分野の知識を網羅的に広げること。難しい論文は後送りにするが除外はしない。
- ユーザー側の入力は、既存の `[読了]/[途中]/[未読]` 報告と詰まり箇所の記述のみ。新たな判断作業を課さない。

---

## 修正1: 出題時の範囲内自己検証（優先度: 最高）

### 目的
「指定範囲に答えがない設問」を構造的に排除する。背景1の根治。

### 方針
プロンプトによる制約は効かない。全文を読んだ状態のモデルは、自分の知識が範囲内由来か範囲外由来かを切り分けられないため。**逐語引用を強制することで検証可能にする。**

### 変更内容
`generate_delivery_and_quiz` の出力スキーマに、設問ごとのフィールドを追加する。

- `evidence_quotes`: その設問の答えの根拠となる文を、**指定範囲から逐語で**引用した配列（1〜3件）
- `evidence_sections`: 各引用の出所（節番号・式番号）

生成後、以下を検証する。

- 引用が `assigned_sections` の範囲内から取られているか
- 設問が要求する論点すべてに対応する引用が存在するか（複数論点を持つ設問は論点ごとに引用を要求する）

引用できない設問は**破棄して再生成**するか、その論点が含まれる節を `assigned_sections` に追加する。

### 補足
コスト増はほぼない（プロンプトと出力スキーマの変更のみ、PDF再アップロード不要）。

---

## 修正2: 採点時の本文照合（優先度: 高）

### 目的
模範解答と表現が異なるが実質正しい回答を、誤答として扱わないようにする。

### 現状の問題
`grade_single` は `key_points` と `model_answer` にしか照らしていないため、言い回しが違う正答や、要点に含まれない細部の正誤を取りこぼす。

### 変更内容
修正1で得た `evidence_quotes` を `grade_single` に渡し、**模範解答との一致ではなく本文との整合**で正誤を判定させる。

- 渡すのは該当設問の引用（数文）のみ。PDF全体は渡さない
- 採点プロンプトに「模範解答と表現が異なっても、`evidence_quotes` と整合していれば正答とする」旨を明示
- 誤りを指摘する際は、根拠として本文の該当箇所（節・式番号）を提示させる

### 補足
コスト増は1設問あたり数文ぶん。修正1とセットで実装すれば追加取得は不要。

---

## 修正3: `[途中]/[未読]` での延長分岐（優先度: 高）

### 目的
3日で読み切れなかった場合に、未理解のまま次に進まないようにする。

### 現状の問題
`cycle_day` が 1→2→3 でハードコードされており、回答タグ（`[読了]/[途中]/[未読]`）を取得しているのに分岐に使っていない。

### 変更内容
`morning.py` の `cycle_day` 固定を解除し、回答タグで分岐する。

- `[読了]` → 次の日へ進む
- `[途中]` → 同じ範囲・同じ設問を再配信（`cycle_day` を進めない）。詰まり箇所の記述があれば、それに対応する解説を添える
- `[未読]` → 範囲を分割して再配信（1日分の分量を半分にする）

サイクル長を可変にし、4日・5日かかる論文を許容する。キューが詰まることは許容する（未理解の論文を消化済みにする方が損失が大きい）。

---

## 修正4: 前提知識の提示と読み飛ばし指示（優先度: 高）

### 目的
背景2の根治。時間の延長では解決しないため、前提知識の欠落を配信前に埋める。

### 変更内容

**(a) `prerequisites` フィールドの追加**

`generate_delivery_and_quiz` に、その論文を読むために必要な前提概念のリストを出力させる（例: 微分逆インダクタンス行列、非線形可観測性、二次平均化）。Day1の配信に提示する。

**(b) Day 0 の追加**

未習の前提概念がある場合、論文本体の前に**前提知識の確認・補充だけを行う日**を挿入する。読むべき入門的資料・レビュー論文はシステム側が指定する（ユーザーに探させない）。これにより論文本体が4日サイクルになるのは許容する。

Day 0 は複数日に分割してよい（Day 0-1, Day 0-2, ...）。前提概念は1つずつ独立して積めるため分割による損失が小さく、途中で止まっても次の論文に持ち越せる。

**(b-1) Day 0 の目標水準 — 「概念の習得」ではなく「その論文で詰まらない最小限の直観」**

ここを定義しないと Day 0 が無限に伸びる。厳密な理解を目標にしてはならない。

実例として、背景2の論文で実際に必要だったのは以下の水準である。

- 非線形可観測性 → 「測定量から状態が一意に決まるかどうか。ないならアルゴリズムが悪いのではなく情報が存在しない」
- 微分逆インダクタンス行列 → 「電圧を電流に写す行列だから、方向ごとの電流の通しやすさ」
- 座標変換のサンドイッチ構造（M X Mᵀ）→ 「d-q に回して物理法則を当てて戻している」

いずれも1概念あたり数分で足りる。厳密にやれば講義1学期分になる概念でも、この水準なら15分に3〜4個収まる。

実装上の制約として、**Day 0 の出力は1概念あたり2〜3文に制限する。** 教科書的な解説を生成させると必ず15分を超える。`prerequisites` には、その論文を読むのに必要な直観のレベルを併記させる。

**(b-2) Day 0 の上限と超過時の処理**

Day 0 が3日を超える場合（前提概念が5個以上、または数学的道具そのものが未習の場合）は、**その論文をキューの後ろに戻し、同じ前提概念を扱うより易しい論文を先に配信する**。修正5のキュー順序制御をそのまま使う。論文は捨てない。

**(c) `skip_sections` フィールドの追加**

初読では飛ばしてよい箇所を明示させる（例: 平均化定理の適用を厳密に示す証明部分は、結論だけ使えば先に進める）。読書プランに「どこを読むか」と並べて「どこを読まないか」を記載する。

### 補足
(c) の効果は大きい。背景2の事例では、証明部分を飛ばす指示があるだけで当日の負荷が大幅に下がっていた。

---

## 修正5: キュー順序の自動制御（優先度: 中）

### 目的
依存関係を無視した順序で配信されることによる停滞を防ぐ。ユーザーに判断を持たせずに実現する。

### 変更内容
キュー投入・配信選択の段階で、以下を自動判定する。

- **難易度型の推定**: 出典（IEEE TIE/TIA 系か Automatica / arXiv math.OC 系か）、著者の所属分野、Abstractのキーワード（averaging, observability, Lyapunov, port-Hamiltonian 等が出れば理論型）から、実務型／理論型を分類する
- **前提知識の突き合わせ**: `prerequisites` を修正6の台帳と照合し、未習概念の数を算出する
- **順序制御**: 未習が多い論文はキュー内で後送りにし、その未習概念を扱う入門的論文・レビュー論文を先に配信する

**捨てる判断は行わない。並べ替えのみ。** ユーザーには「今日はこれ」としか見えないようにする。

難易度型は配信時に表示してよい（分単位の精密な見積もりは表示しない。外れた場合に負荷になるため）。

---

## 修正6: 概念台帳（優先度: 中、実装量は最大）

### 目的
既習／未習の概念を蓄積し、修正4・5の判断材料にする。15分×3日に到達するための本体。

### 変更内容
遭遇した前提概念を記録するストアを持つ。

- 論文ごとの `prerequisites` を登録
- 状態を「未習 / 遭遇したが詰まった / 既習」で管理
- **更新のセンサーは既存の日次報告**。`[途中]` + 詰まり箇所の記述（例: 「式(29)の S が何なのか分からない」）から、該当概念を未習として記録する。ユーザーに新たな判断や入力を求めない
- 同じ概念が複数論文で再登場することを利用し、在庫が溜まるにつれ Day 0 の必要性が減る想定

### 進捗指標の出力

台帳から以下2つを算出し、定期的に提示する。到達までの期間を推測で気にするのではなく、観測で判断できるようにするため。

- **1本あたりの新規前提概念の数** — 前提知識の在庫がどれだけ蓄積したかを示す。減少していけば順調
- **`[読了]` と `[途中]` の比率** — 実力の指標

両方の曲線が寝てくれば到達間近と判断する。寝ない場合は、配信されている論文の型が現在の水準に対して難しすぎるため、修正5のキュー順序制御を調整する。

### 補足
汎用概念（可観測性、座標変換のサンドイッチ構造、逆インダクタンス行列など）は分野内の多くの論文で再登場するため、投資効率が高い。個別論文の証明を追うより優先して記録する価値がある。

なお、目的が関連分野の網羅であるため、新しいサブ分野に入るたびに分野固有の概念は積み直しになる（数学的道具は持ち越せる）。「全分野で15分」への到達は想定せず、**サブ分野ごとに順次到達していく**ものとして扱う。

---

## 実装順序

1. 修正1（範囲内自己検証） — 低コストで背景1を根治
2. 修正2（本文照合採点） — 修正1の成果物を使う
3. 修正3（延長分岐） — ハードコード1箇所の変更で効果大
4. 修正4（prerequisites / skip / Day 0） — 背景2を根治
5. 修正6（概念台帳） — 修正5の前提
6. 修正5（キュー順序制御）

## 変更しない項目

- 1日15分の時間設定（実務型論文には妥当。問題は一律適用ではなく難度推定の欠如にあった）
- 分単位の所要時間見積もりの精密化（外れた場合の負荷が大きく、効果も限定的）
- ユーザーによる論文の取捨選択（設計上の制約を参照）
