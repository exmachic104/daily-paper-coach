name: hello-diagnostic

on:
  push: {}
  workflow_dispatch: {}

jobs:
  hello:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Actions is working. If you can see this run, the platform is fine."
